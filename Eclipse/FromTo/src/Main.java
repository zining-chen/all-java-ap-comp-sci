import java.util.Scanner;

public class Main { //from year to year

	public static void main(String[] args) {
		
		Scanner kb = new Scanner(System.in);
		int startYr = Integer.parseInt(kb.nextLine());
		String startYear = startYr + "";
		int endYr = startYr + 1;
		boolean status;
		
		while ( status = false  ) {
			
			for (int i = 0; i < startYear.length(); i ++){
				
			}
			
			/*  commented out for now might reuse
			if (endYr =  ) // somehow figure out no digits
				status = true; // found it
		}
		
		
		int thousands = Integer.parseInt(a, kb.nextInt());
		int hundreds = Integer.parseInt(a, kb.nextInt());
		int tens = Integer.parseInt(a, kb.nextInt());
		int ones = Integer.parseInt(a, kb.nextInt());
		int secondYear = startYear;

		while ( (b.indexOf(charAt(1)) == -1) && (b.indexOf(charAt(2)) == -1) && ((b.indexOf(3) == -1) && (a.indexOf(4) == -1) ) )  {
			secondYear = secondYear + 1;
			b = secondYear + "";
		}
		
		
		if ( (a.indexOf(thousands) != -1) || (a.indexOf(hundreds) != -1) || (a.indexOf(tens) != -1) || (a.indexOf(ones) != -1) ) {
			secondYear ++;
			String temp = secondYear + "";
			thousands = 
					
		}
		
		*/
		
		
		
		

		String b = endYr + "";
		System.out.println( b );
	}
}
}
